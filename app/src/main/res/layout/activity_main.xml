<ScrollView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".MainActivity">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical"
        android:padding="16dp">

        <TextView
            android:id="@+id/tv_title"
            android:text="@string/tv_title"
            style="@style/TitleStyle"/>

        <Button
            android:id="@+id/btn_spotify_streamer_app"
            android:text="@string/btn_spotify_streamer_app"
            style="@style/ButtonStyle"/>

        <Button
            android:id="@+id/btn_scores_app"
            android:text="@string/btn_scores_app"
            style="@style/ButtonStyle"/>

        <Button
            android:id="@+id/btn_library_app"
            android:text="@string/btn_library_app"
            style="@style/ButtonStyle"/>

        <Button
            android:id="@+id/btn_build_it_bigger_app"
            android:text="@string/btn_build_it_bigger_app"
            style="@style/ButtonStyle"/>

        <Button
            android:id="@+id/btn_xyz_reader_app"
            android:text="@string/btn_xyz_reader_app"
            style="@style/ButtonStyle"/>

        <Button
            android:id="@+id/btn_capstone_app"
            android:text="@string/btn_capstone_app"
            style="@style/ButtonStyle"/>

    </LinearLayout>
</ScrollView>
